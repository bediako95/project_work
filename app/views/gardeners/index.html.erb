
<p id="notice"><%= notice %></p>

<h1>Gardeners</h1>

<table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Phone</th>
      <th>Residential address</th>
      <th>Ratings</th>
      <th>Actions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gardeners.each do |gardener| %>
      <tr>
        <td><%= gardener.first_name %></td>
        <td><%= gardener.last_name %></td>
        <td><%= gardener.phone %></td>
        <td><%= gardener.residential_address %></td>
        <td>
        <%= form_tag 'Request', onsubmit: "foo(return false);" do %>

<% end %>
  </td>
       
   <td> <%= #button_tag "Request for Service", type: 'button', onclick: "myFunction()", class: 'btn btn-success',data: { confirm: 'Are you sure you want to request for his service?' } %></td>    
<%# link_to 'Request for Service',post_sample_path() ,class:'btn btn-success',%>





      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%if users_authentication_signed_in?%>
<%= link_to 'Back', rails_admin_path, class:"btn btn-primary" %>
 <%else%>
 <%= link_to 'Back', root_path, class:"btn btn-primary" %>
 <%end%>

